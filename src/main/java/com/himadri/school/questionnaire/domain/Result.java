package com.himadri.school.questionnaire.domain;

import lombok.Data;
import lombok.Getter;
import lombok.Setter;

import javax.persistence.Entity;
import javax.persistence.Table;

/*@Getter
@Setter
@Data
@Entity
@Table( name = "result")*/
public class Result {

    /**
     * this has to be user/client specifc.
     * We need to thing of somwthing where we can just display the result without saving it to the database.
     * Later we can create something that we will save on the database.
     *
     */
}
